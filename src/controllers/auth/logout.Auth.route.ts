import IRouteParameters_options from "../../types/RouteParams.type";

const logoutUser = async () => {
  // try {
  //   res.send("Logout Route");
  // } catch (error) {
  //   return next(error);
  // }
};

export default logoutUser;
